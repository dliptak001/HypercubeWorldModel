# Python wheels build the C++ sources at the repo root in-tree with the
# extension. Packaging root: python/.
#
# PyPI trusted publisher (OIDC, no API token) — set up once before first tag:
#   Project:  hypercube-worldmodel
#   Owner:    dliptak001
#   Repo:     HypercubeWorldModel
#   Workflow: wheels.yml
#
# Publish runs only on tags matching v* (e.g. v1.0.0). skip-existing avoids
# failing when re-running a tag after a partial upload. The same job then
# creates the GitHub Release for the tag with the wheels and sdist attached
# and auto-generated notes; a tag alone does not create a Release.

name: Build wheels

on:
  push:
    branches: [master]
    tags: ["v*"]
  pull_request:
    branches: [master]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build_wheels:
    name: Wheels on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-14]

    steps:
      - uses: actions/checkout@v6

      # Register binfmt so cibuildwheel can build Linux aarch64 wheels via emulation.
      - name: Set up QEMU
        if: runner.os == 'Linux'
        uses: docker/setup-qemu-action@v3
        with:
          platforms: arm64

      - uses: pypa/cibuildwheel@v4.0.0
        with:
          package-dir: python
          output-dir: wheelhouse

      - uses: actions/upload-artifact@v7
        with:
          name: wheels-${{ matrix.os }}
          path: wheelhouse/*.whl

  build_sdist:
    name: Source distribution
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      # Archives the python/ package tree for PyPI completeness. From-source
      # installs still need a full repo clone (the C++ sources sit outside
      # python/) — see docs/Python_SDK.md.
      - run: pipx run build --sdist python

      - uses: actions/upload-artifact@v7
        with:
          name: sdist
          path: python/dist/*.tar.gz

  publish:
    name: Publish to PyPI
    needs: [build_wheels, build_sdist]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      id-token: write
      contents: write
    steps:
      - uses: actions/download-artifact@v8
        with:
          path: dist
          merge-multiple: true

      - uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: dist
          skip-existing: true

      - uses: softprops/action-gh-release@v2
        with:
          name: HypercubeWorldModel ${{ github.ref_name }}
          files: dist/*
          generate_release_notes: true
